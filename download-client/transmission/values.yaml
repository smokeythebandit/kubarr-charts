# Default values for transmission
# This is a YAML-formatted file.

# Namespace configuration
namespace:
  create: true
  name: transmission

# Transmission application configuration
transmission:
  replicaCount: 1

  image:
    repository: linuxserver/transmission
    pullPolicy: IfNotPresent
    tag: "4.1.3"

  # Base path for URL routing (leave empty for root)
  basePath: "/transmission"

  # Service configuration
  service:
    type: ClusterIP
    port: 9091
    targetPort: 9091
    annotations: {}

  # Resource limits and requests
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 1000m
      memory: 1Gi

  # Environment variables
  env:
    PUID: "1000"
    PGID: "1000"
    TZ: "Etc/UTC"

  # Download paths - set dynamically based on storage.mountPath
  # These are injected via the configmap template
  downloadDir: "/data/downloads/transmission"
  incompleteDir: "/data/downloads/transmission/incomplete"

  # Probes configuration
  livenessProbe:
    enabled: true
    tcpSocket:
      port: 9091
    initialDelaySeconds: 30
    periodSeconds: 30
    timeoutSeconds: 10
    failureThreshold: 3

  readinessProbe:
    enabled: true
    tcpSocket:
      port: 9091
    initialDelaySeconds: 15
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 3

# Security context
securityContext:
  runAsNonRoot: false
  allowPrivilegeEscalation: true
  capabilities: {}
  readOnlyRootFilesystem: false

# Pod security context
podSecurityContext:
  fsGroup: 1000
  seccompProfile:
    type: RuntimeDefault

# Service Account
serviceAccount:
  create: true
  name: transmission
  annotations: {}

# Node selector
nodeSelector: {}

# Tolerations
tolerations: []

# Affinity
affinity: {}

# Pod annotations
podAnnotations: {}

# Image pull secrets
imagePullSecrets: []

# Storage configuration. All app storage is routed through the shared NFS claim.
storage:
  media:
    existingClaim: media-data
    mountPath: /data
    configMountPath: /config
    configSubPath: config/transmission
    downloadsMountPath: /downloads
    downloadsSubPath: downloads/transmission

# VPN Configuration
# When enabled, a Gluetun sidecar container is added to route all traffic through VPN
vpn:
  enabled: false
  secretName: ""
  image:
    repository: qmcgaw/gluetun
    tag: "v3.41.3"
    pullPolicy: IfNotPresent
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
  killSwitch: true
  firewallOutboundSubnets: ""
  # Port forwarding via NAT-PMP (supported by ProtonVPN, PIA, AirVPN)
  portForwarding:
    enabled: false
    # Command to run when a new forwarded port is assigned ({{PORT}} is replaced by Gluetun)
    upCommand: transmission-remote localhost:9091 -p {{PORT}}

# Network policy configuration
networkPolicy:
  enabled: true
  ingressFrom:
    - openresty  # Kubarr gateway proxy
    - sonarr
    - radarr
    - victoriametrics
  egressTo: []

# Metrics exporter sidecar (auto-scraped by VictoriaMetrics when enabled)
# NOTE: metalmatze/transmission-exporter is outdated and crashes on newer Transmission versions
exporter:
  enabled: false
  image:
    repository: metalmatze/transmission-exporter
    tag: "0.3.0"
    pullPolicy: IfNotPresent
  port: 19091
  resources:
    requests:
      cpu: 10m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 64Mi
# Vertical Pod Autoscaler. Requires the VPA operator/CRD in the cluster.
vpa:
  enabled: false
  updateMode: Auto
  containerName: "*"
  controlledResources:
    - cpu
    - memory
  minAllowed: {}
  maxAllowed: {}
