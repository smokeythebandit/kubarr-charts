apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "plex.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "plex.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.plex.replicaCount }}
  selector:
    matchLabels:
      {{- include "plex.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "plex.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "plex.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- if .Values.vpn.enabled }}
      initContainers:
        {{- include "kubarr-common.gluetun.initContainer" (dict "root" .) | nindent 8 }}
      {{- end }}
      containers:
        {{- include "kubarr-common.gluetun.container" (dict "root" . "appPort" .Values.plex.service.targetPort) | nindent 8 }}
        - name: plex
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.plex.image.repository }}:{{ .Values.plex.image.tag }}"
          imagePullPolicy: {{ .Values.plex.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.plex.service.targetPort }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "plex.fullname" . }}-config
          {{- if .Values.plex.livenessProbe.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.plex.livenessProbe.httpGet.path }}
              port: {{ .Values.plex.livenessProbe.httpGet.port }}
            initialDelaySeconds: {{ .Values.plex.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.plex.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.plex.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.plex.livenessProbe.failureThreshold }}
          {{- end }}
          {{- if .Values.plex.readinessProbe.enabled }}
          readinessProbe:
            httpGet:
              path: {{ .Values.plex.readinessProbe.httpGet.path }}
              port: {{ .Values.plex.readinessProbe.httpGet.port }}
            initialDelaySeconds: {{ .Values.plex.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.plex.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.plex.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.plex.readinessProbe.failureThreshold }}
          {{- end }}
          resources:
            {{- toYaml .Values.plex.resources | nindent 12 }}
          volumeMounts:
            {{- include "kubarr-common.storage.configMount" (dict "root" .) | nindent 12 }}
            {{- include "kubarr-common.storage.mediaMount" (dict "root" . "mountPath" .Values.storage.media.transcodeMountPath "subPath" .Values.storage.media.transcodeSubPath) | nindent 12 }}
            {{- include "kubarr-common.storage.mediaMount" (dict "root" . "mountPath" .Values.storage.media.mountPath "subPath" .Values.storage.media.dataSubPath "readOnly" .Values.storage.readOnly) | nindent 12 }}
      volumes:
        {{- include "kubarr-common.gluetun.volumes" (dict "root" .) | nindent 8 }}
        {{- include "kubarr-common.storage.configOrMediaVolume" (dict "root" . "fullname" (include "plex.fullname" .)) | nindent 8 }}
      {{- include "kubarr-common.scheduling" . | nindent 6 }}
